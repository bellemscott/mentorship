require "test_helper"

class MatchingTest < ActiveSupport::TestCase
    # create 2 users, one mentee one mentor, make sure they have fields they can match on (length of mentorship)
    # call match create/new action in controller
    # assert that the match record contains the correct information
end